trigger:
- master

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build  
  displayName: Build image
  jobs:
  - job: Build_and_Push
    displayName: Build and Push
    pool: rpi-cluster
    steps:
    - task: Docker@2
      inputs:
        containerRegistry: 'dockerhub_mactat'
        repository: 'map-pacer-agent'
        command: 'buildAndPush'
        Dockerfile: 'agent/Dockerfile'
        buildContext: '.'
        tags: 'arm-latest'
