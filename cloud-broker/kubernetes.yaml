apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-broker
  labels:
        app: cloud-broker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloud-broker
  template:
    metadata:
      labels:
        app: cloud-broker
    spec:
      containers:
      - name: cloud-broker-container
        image: hivemq/hivemq-ce
        resources:
          limits:
            memory: "0.6Gi"
            cpu: "300m"  
          requests:
            memory: "0.2Gi"
            cpu: "150m"  
        ports:
        - containerPort: 1883

---
apiVersion: v1
kind: Service
metadata:
  name: cloud-broker-service
spec:
  selector:
    app: cloud-broker 
  ports:
  - port: 1883
    targetPort: 1883

# Add Ingress!